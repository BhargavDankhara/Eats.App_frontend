import { Button } from "./ui/button";

export default function MainNave() {
  return (
    <Button variant="ghost" className="font-bold hover:text-orange-500 hover:bg-white text-xl">
        Log In 
    </Button>
  )
}
